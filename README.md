This project hosts static JSON data for use in Azure Data Factory pipelines.
Files are stored under the /data folder and served through Azure Static Web Apps.
Each JSON file can be accessed via a public URL for batch ingestion.
Structure: index.html + /data/*.json
